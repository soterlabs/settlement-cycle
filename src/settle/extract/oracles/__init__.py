"""On-chain oracle readers — Chronicle, Chainlink, Pyth, Redstone, etc.

Each module wraps one oracle protocol's read pattern. Used by the
``normalize.sources.oracles`` source implementations to build the
``INavOracleSource`` Protocol.
"""
