# Copy to `.env` and fill in your own keys. `.env` is gitignored.
#
# Loaded automatically at `import settle` time (via python-dotenv).
# A real environment variable in the shell still wins — `.env` is just a
# convenience for local runs.

# Per-chain RPC endpoints. Names match `RPC_ENV_VARS` in extract/rpc.py;
# adding a new chain requires updating that mapping AND the Chain enum in
# domain/primes.py.
ETH_RPC=https://eth-mainnet.g.alchemy.com/v2/<your_key>
BASE_RPC=https://base-mainnet.g.alchemy.com/v2/<your_key>
AVALANCHE_C_RPC=https://avax-mainnet.g.alchemy.com/v2/<your_key>
MONAD_RPC=https://monad-mainnet.g.alchemy.com/v2/<your_key>
PLUME_RPC=https://rpc.plumenetwork.xyz
# Needs ARCHIVE access for the spUSDG S2 venue's totalAssets reads (S63);
# Alchemy serves full Robinhood archive. The official public endpoint
# (https://rpc.mainnet.chain.robinhood.com) keeps only minutes of state —
# fine for block resolution / latest reads only; if no archive endpoint
# is available, set S63 `total_assets_source: hypersync_underlying`
# (HyperSync Transfer-log reconstruction, ~0.06% understatement).
ROBINHOOD_RPC=https://robinhood-mainnet.g.alchemy.com/v2/<your_key>

# Dune API key for live (non-fixture) Dune source paths.
DUNE_API_KEY=

# Postgres raw-data store (optional — pipeline runs pickle-only when unset).
# Provisioned on Railway: `railway add --plugin postgres`, then
# `railway variables` exposes this DATABASE_URL. Apply the schema with
# `psql "$DATABASE_URL" -f db/schema.sql` once after provisioning.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=
